[build]
  base = "creative_infotech"           
  publish = "creative_infotech/build" 
  command = "npm install && npm run build"

[build.environment]
  NODE_VERSION = "18"
  NODE_OPTIONS = "--openssl-legacy-provider"
